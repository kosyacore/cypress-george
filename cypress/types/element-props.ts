/**
 * ElementProps is a type that describes the properties of an element
 * @param name name of the element
 * @param locator locator of the element
 */
export type ElementProps = {
    name: string;
    locator: string;
};